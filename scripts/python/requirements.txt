# SISTAC — Dependencias Python
# Instalar con: pip install -r requirements.txt
# Python >= 3.10 recomendado

# ====== LLM: Anthropic Claude ======
anthropic>=0.28.0              # SDK oficial de Anthropic (Claude API)
langchain>=0.2.0
langchain-anthropic>=0.2.0     # Integración LangChain ↔ Claude (para RAGAS)
langchain-community>=0.2.0
langchain-text-splitters>=0.3.0  # RecursiveCharacterTextSplitter (chunking)
openai>=1.35.0                 # SDK OpenAI (David) + embeddings text-embedding-3-small

# ====== RAG: Vector Store y Embeddings ======
# Azure AI Search: vector store oficial del experimento (créditos estudiante)
# Configurar: AZURE_SEARCH_ENDPOINT, AZURE_SEARCH_KEY en .env
# chromadb>=0.5.0              # Alternativa local (sin Azure)
sentence-transformers>=3.0.0   # Embeddings locales cuando LLM_PROVIDER=anthropic

# ====== Evaluación RAG (sección 5.6) ======
ragas>=0.1.0                   # RAGAS evaluation framework (faithfulness, relevancy, precision)

# ====== Detección y Anonimización PII (Mario, H3) ======
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7.0
# Modelo spaCy español: python -m spacy download es_core_news_lg

# ====== Evaluación y Métricas ======
# scikit-learn: SOLO para métricas de clasificación (F1, AUC-ROC)
# NO usar para inferencia causal (ver CLAUDE.md Rule Overrides)
scikit-learn>=1.5.0
scipy>=1.13.0                  # Mann-Whitney U, bootstrap CI, Chi-cuadrado

# ====== Procesamiento de Datos ======
pandas>=2.2.0
numpy>=1.26.0
python-docx>=1.1.0             # Extracción de texto de .docx
pdfplumber>=0.11.0             # Extracción de texto de PDF nativo (gratis)
google-genai>=1.0.0            # Gemini 2.5 Flash: PDF escaneado + imágenes OCR
# ====== Web Application ======
fastapi>=0.111.0               # API REST de la aplicación SISTAC
uvicorn>=0.30.0                # Servidor ASGI
python-multipart>=0.0.9        # Upload de archivos en FastAPI

# ====== Generación de Datos Sintéticos ======
sdv>=1.14.0                    # Synthetic Data Vault (PrivBayes wrapper)
faker>=25.0.0                  # Nombres/datos sintéticos en español

# ====== Visualización ======
matplotlib>=3.8.0
seaborn>=0.13.0
openpyxl>=3.1.0

# ====== Calidad de Código ======
pytest>=8.0.0
black>=24.0.0
ruff>=0.4.0

# ====== Utilidades ======
python-dotenv>=1.0.0           # Variables de entorno (ANTHROPIC_API_KEY)
tqdm>=4.66.0                   # Barras de progreso
loguru>=0.7.0                  # Logging estructurado
pymongo>=4.6.0                 # Driver de MongoDB para persistencia de datos

